#include <iostream>
using namespace std;

int solution(int num1, int num2) {
    return num1 * num2;
}

int main() {
    int a, b;
    cin >> a >> b;
    cout << solution(a, b) << endl;
    return 0;
}
